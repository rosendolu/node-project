# node-project

Nodejs facilitated project

## Test

```sh
# test all
node --test

# test pattern && watch
node --test --test-name-pattern="binary-search" --watch

# test dedicated file
node --test "**/**binary-search**"
```
